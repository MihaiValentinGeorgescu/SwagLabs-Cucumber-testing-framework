Feature: Sort items functionality
  Scenario: User logs in and then presses the sort button and sorts them in ascending order
    Given User logs in on the website
    When User presses sort button
    Then User sees items sorted